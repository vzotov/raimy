# Next.js Frontend Environment Variables

# Backend API URL (for server-side requests from Next.js to Python backend)
# - Used by: middleware, route handlers, server components
# - Development: http://localhost:8000
# - Production: http://<GCP_VM_IP>/api (e.g., http://35.207.14.249/api)
# Note: Client-side code uses relative paths like /api/* which are rewritten by Next.js
API_URL=http://localhost:8000
